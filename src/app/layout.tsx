import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "RescuEdge | Premium Intelligent Geo-Tagging & Video Surveillance",
  description: "RescuEdge is an enterprise-grade intelligent video surveillance and geotagging dashboard providing real-time AI computer vision highlights and incident monitoring.",
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
